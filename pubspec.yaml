name: flexi_bottom_nav
description: "A flexible Flutter bottom navigation widget with customizable icons, labels, and colors to tailor the navigation bar to your app's needs."
version: 0.0.2
homepage: https://github.com/harshilchovatiya/flexi_bottom_nav

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.20.0
  flutter_lints: ^3.0.0


flutter:
