name: flexi_bottom_nav
description: "FlexiBottomNavigation is a customizable bottom navigation widget for Flutter. It allows you to easily create a bottom navigation bar with configurable icons, labels, and colors, giving you the flexibility to tailor the navigation experience to your app's needs."
version: 0.0.2
homepage: https://github.com/harshilchovatiya/flexi_bottom_nav

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.20.0
  flutter_lints: ^3.0.0


flutter:
